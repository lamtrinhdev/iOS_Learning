# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'Weather' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Weather
  pod 'Alamofire', '~> 4.9'                     # Networking
  pod 'RxSwift', '~> 5.0'                       # Support code binding
  pod 'RxCocoa', '~> 5.0'                       # Support bind for Cocoa framework

end
